name: Package Central VitalVeg V8

on:
  push:
    branches: [v8-production]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate JavaScript
        run: |
          node --check app.js
          node --check auth.js
          node --check v8.js
          node --check v8-hotfix.js
          node --check v8-delivery-fix.js
          node --check v8-order-intelligence.js
          node --check v8-ops.js
          node --check v8-session.js
          node --check v8-generic-order-lines.js
          node --check v8-print-fix.js
      - name: Package source
        uses: actions/upload-artifact@v4
        with:
          name: vitalveg-central-v8-source
          path: |
            index.html
            print.html
            styles.css
            auth.css
            auth.js
            app.js
            v8.css
            v8.js
            v8-hotfix.css
            v8-hotfix.js
            v8-delivery-fix.js
            v8-order-intelligence.js
            v8-ops.css
            v8-ops.js
            v8-session.js
            v8-generic-order-lines.js
            v8-print-fix.js
            manifest.json
            icon.svg
            sw.js
            netlify.toml
            package.json
            package-lock.json
            netlify/functions/**
          if-no-files-found: error
          retention-days: 1
